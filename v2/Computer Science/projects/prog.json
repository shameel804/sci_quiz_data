[
    {
        "title": "Variables and Types 📦",
        "ques": "In Python:\n1. Declare integer, string, boolean\n2. What is dynamic typing?\n3. Check type of a variable",
        "answer": {
            "type": "code",
            "lang": "python",
            "content": "# 1. Variable declarations\nage = 25          # integer\nname = \"Alice\"    # string\nis_student = True # boolean\n\n# 2. Dynamic typing: Type determined at runtime,\n#    can change during execution\nage = \"twenty-five\"  # Now it's a string!\n\n# 3. Check type\nprint(type(age))  # <class 'str'>"
        },
        "explanation": "**Python is dynamically typed** - no need to declare types explicitly. Contrast with Java/C++."
    },
    {
        "title": "Control Structures 🔀",
        "ques": "Write code for:\n1. If-else statement\n2. For loop (1 to 5)\n3. While loop (countdown)",
        "answer": {
            "type": "code",
            "lang": "python",
            "content": "# 1. If-else\nage = 18\nif age >= 18:\n    print(\"Adult\")\nelse:\n    print(\"Minor\")\n\n# 2. For loop\nfor i in range(1, 6):\n    print(i)  # 1, 2, 3, 4, 5\n\n# 3. While loop\ncount = 5\nwhile count > 0:\n    print(count)\n    count -= 1\nprint(\"Liftoff!\")"
        },
        "explanation": "**Control structures** direct program flow. Use for when count known, while when condition-based."
    },
    {
        "title": "Functions 🔧",
        "ques": "1. Define a function with parameters\n2. Return value\n3. Default parameters",
        "answer": {
            "type": "code",
            "lang": "python",
            "content": "# 1. Function with parameters\ndef greet(name):\n    print(f\"Hello, {name}!\")\n\n# 2. Return value\ndef add(a, b):\n    return a + b\n\nresult = add(3, 4)  # result = 7\n\n# 3. Default parameters\ndef greet(name=\"World\"):\n    return f\"Hello, {name}!\"\n\nprint(greet())        # Hello, World!\nprint(greet(\"Alice\")) # Hello, Alice!"
        },
        "explanation": "**Functions** are reusable code blocks. Parameters make them flexible; return sends values back."
    },
    {
        "title": "OOP Basics 🏗️",
        "ques": "1. What is a class?\n2. Create a simple class\n3. Instantiate an object",
        "answer": {
            "type": "code",
            "lang": "python",
            "content": "# 1. Class: Blueprint for creating objects\n\n# 2. Simple class\nclass Dog:\n    def __init__(self, name, age):\n        self.name = name  # Attribute\n        self.age = age\n    \n    def bark(self):  # Method\n        return f\"{self.name} says Woof!\"\n\n# 3. Instantiate\nmy_dog = Dog(\"Buddy\", 3)\nprint(my_dog.name)   # Buddy\nprint(my_dog.bark()) # Buddy says Woof!"
        },
        "explanation": "**OOP** organizes code around objects. Classes define attributes (data) and methods (behavior)."
    },
    {
        "title": "Inheritance 🧬",
        "ques": "1. What is inheritance?\n2. Create parent and child class\n3. Override a method",
        "answer": {
            "type": "code",
            "lang": "python",
            "content": "# 1. Inheritance: Child class inherits from parent\n\n# 2. Parent class\nclass Animal:\n    def __init__(self, name):\n        self.name = name\n    def speak(self):\n        return \"Some sound\"\n\n# Child class\nclass Cat(Animal):\n    # 3. Override method\n    def speak(self):\n        return f\"{self.name} says Meow!\"\n\ncat = Cat(\"Whiskers\")\nprint(cat.speak())  # Whiskers says Meow!"
        },
        "explanation": "**Inheritance** promotes code reuse. Child classes can override parent methods."
    },
    {
        "title": "Error Handling ⚠️",
        "ques": "1. What are exceptions?\n2. Try-except block\n3. Handle specific exception",
        "answer": {
            "type": "code",
            "lang": "python",
            "content": "# 1. Exceptions: Errors during execution\n\n# 2. Try-except\ntry:\n    result = 10 / 0\nexcept:\n    print(\"An error occurred\")\n\n# 3. Specific exception\ntry:\n    num = int(\"abc\")\nexcept ValueError:\n    print(\"Invalid number format\")\nexcept ZeroDivisionError:\n    print(\"Cannot divide by zero\")\nfinally:\n    print(\"This always runs\")"
        },
        "explanation": "**Exception handling** prevents crashes. Always catch specific exceptions when possible."
    }
]